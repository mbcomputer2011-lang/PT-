name: Simple CI Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Build ADK
      run: |
        # ตัวอย่างคำสั่ง (แทนที่ด้วยคำสั่งที่ใช้สร้างไฟล์ ADK จริง)
        mkdir build
        echo "YOUR ADK CONTENT HERE" > build/myfile.adk

    - name: Upload ADK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ADK-file
        path: build/
